# Zone 1 — Power Restoration & Conveyor Revival

The sorting floor is dead. Motor control center is unresponsive, conveyors
haven't moved in who knows how long. Somewhere in this segment there's a
PLC still listening — it just isn't talking to anyone anymore.

A pcap from the last maintenance shift is included below. It might not
tell the whole story.

Target: `nc`-reachable Modbus TCP endpoint, standard port.
Attachment: `maintenance_session.pcap`

Bring the line back up. The floor will tell you when it's happy.

Flag format: `flag{...}`

## Short Writeup

-
-
-
